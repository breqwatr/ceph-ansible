# breqwatr/ceph-ansible

This is the BWDT image for Ceph-Ansible

## Environment variables

The Dockerfile uses a build-arg to define the release.

```

RELEASE   (required)    Define the git branch that Ceph-Ansible will be
                        installed from.

```
